> bake() should raise on an unknown oven, not print a warning
● (edits it)
> no, raise ValueError, don't return None
● (edits it)
> it's still returning None for "spyre"
● (edits it)
> it's STILL returning None
● (edits a different function)
